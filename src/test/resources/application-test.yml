spring:
  test:
    database:
      replace: none
  docker:
    compose:
      enabled: false

security:
  jwt:
    secret: test-jwt-secret-key-for-unit-testing-only-32chars
    expiration-minutes: 60
